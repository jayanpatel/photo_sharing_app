<%= form_for @post, :html => { :multipart => true} do |f| %>
	<p>
		<%= f.label :caption%> <br />
		<%= f.text_field :caption %>
	</p>
	<p>
		<%= f.file_field :photo %>
	</p>
	<div><%= flash[:error]%></div>
	<p><%= f.submit "Submit"%></p>
<%end%>